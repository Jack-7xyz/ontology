"""Ask Ontology — view-aware Claude API integration (Phase 8).

Exposes `POST /api/ask`: accepts current view context + user message,
returns narrative + optional filter/sort action via a single forced tool call.
"""
