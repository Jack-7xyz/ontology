# Ontology

Shared React app for open-source ontology case studies. The first transferred project is the retail ontology under `src/retail`.

## Development

```bash
npm install
npm run dev
npm run build
npm test
```

Private brand source assets belong in `docs/branding/local-assets/`, which is intentionally ignored.
